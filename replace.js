const fs = require(" fs\); const path = require(\path\); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith(\.html\) || file.endsWith(\.js\)) results.push(file); } }); return results; } const files = walk(\e:/WEB_UTU\); files.forEach(file => { let content = fs.readFileSync(file, \utf8\); if (content.match(/\\bHub\\b|\\bhub\\b/)) { content = content.replace(/\\bLAMURI Toko Penjual\\b/g, \Toko Penjual\).replace(/\\bHub LAMURI\\b/g, \Toko Penjual\).replace(/\\bHub\\b/g, \Toko Penjual\).replace(/\\bhub\\b/g, \gudang mitra\); fs.writeFileSync(file, content, \utf8\); console.log(\Updated \ + file); } });
